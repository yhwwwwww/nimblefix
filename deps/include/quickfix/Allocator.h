#ifndef FIX_ALLOCATOR_H
#define FIX_ALLOCATOR_H

// NimbleFIX keeps QuickFIX as a clean submodule checkout, so provide the default
// empty allocator override from the main repository instead of generating it in
// the submodule worktree.

#endif